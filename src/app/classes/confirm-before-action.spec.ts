import { ConfirmBeforeAction } from './confirm-before-action';

describe('ConfirmBeforeAction', () => {
  it('should create an instance', () => {
    expect(new ConfirmBeforeAction()).toBeTruthy();
  });
});
